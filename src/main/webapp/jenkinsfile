pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                sh './gradlew build'
            }
        }

        stage('Prepare Docs') {
            steps {
                sh './gradlew prepareDocs'
            }
        }
    }
}
